[env:fnk0104b]
board = fnk0104b
board_build.partitions = firmware/partitions_16mb.csv

build_src_filter = ${env.build_src_filter} +<../boards/fnk0104b>
build_flags =
	${env.build_flags}
	-Ifirmware/boards/fnk0104b

lib_deps =
	${env.lib_deps}
